# TensorFlow OCR model stub
from typing import Optional

def load_model(path: str):
    return None  # replace with tf.keras.models.load_model(path)


def infer_plate_text(model, roi_image) -> Optional[str]:
    # Return a Zimbabwe-like plate format placeholder
        return "AB·7123CD"
