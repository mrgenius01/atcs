# Simple preprocess stub: no external dependencies for MVP
# Simulated image preprocessing for Zimbabwe plate recognition

def preprocess_image(img_bytes: bytes) -> dict:
    """Simulate image preprocessing without external dependencies"""
    return {
        "status": "preprocessed",
        "size": len(img_bytes),
        "format": "simulated"
    }
